import { Sedan, Crimson_Text } from "next/font/google";

export const sedan = Sedan({
  subsets: ['latin'],
  weight: ['400']
})

export const crimson = Crimson_Text({
  subsets: ['latin'],
  weight: ['700']
})
